<form method="post" action="{{ path('app_mikroskop_delete', {'id': mikroskop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mikroskop.id) }}">
    <button class="btn">Delete</button>
</form>
