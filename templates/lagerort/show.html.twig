{% extends 'base.html.twig' %}

{% block title %}Lagerort{% endblock %}

{% block body %}
    <h1>Lagerort</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lagerort.id }}</td>
            </tr>
            <tr>
                <th>Kurzbeschreibung</th>
                <td>{{ lagerort.Kurzbeschreibung }}</td>
            </tr>
            <tr>
                <th>Etage</th>
                <td>{{ lagerort.Etage }}</td>
            </tr>
            <tr>
                <th>Raum</th>
                <td>{{ lagerort.Raum }}</td>
            </tr>
            <tr>
                <th>Schranknummer</th>
                <td>{{ lagerort.Schranknummer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lagerort_index') }}">back to list</a>

    <a href="{{ path('app_lagerort_edit', {'id': lagerort.id}) }}">edit</a>

    {{ include('lagerort/_delete_form.html.twig') }}
{% endblock %}
